source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# plugin 'whatsup', path: './lib/'

# Specify your gem's dependencies in bundle-whatsup.gemspec
gemspec
